export const endpoints = {
  health: "/api/health",
  releases: "/api/releases",
  artists: "/api/artists",
  tracks: "/api/tracks",
  search: "/api/search",
  playbackToken: "/api/playback/token",
} as const;
